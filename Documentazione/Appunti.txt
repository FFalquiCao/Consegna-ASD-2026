Primo obiettivo: capire la consegna.
  Oss: ai percorsi non viene data una descrizione formale. Se un percorso potesse ripassare più volte per lo stesso nodo/arco, allora la consegna opzionale non ha senso (avrebbe un valore infinito). Chiedere spiegazioni al docente/verificare se la definizione di percorso è data nel corso. L'esempio del documento non risolve l'ambiguità di voler vietare percorsi che ripassano per uno stesso nodo o percorsi che ripassano per uno stesso arco.
  Oss: per ora ho intenzione di scrivere la porzione di algoritmo che lavora direttamente sul grafo in C++, ma bisogna valutare se voglio usare un altro linguaggio per rendere i file *.all-paths.bz2 più comodi da leggere.
  Oss: bisogna aggiungere l'utente robertogrossi nella repository.
  Oss: il file *.all-paths.bz2 più piccolo ha circa 6*10^5 righe, ciascuna delle quali ha un percorso di circa 4 nodi. Questo fa sì che circa qualsiasi algoritmo che runna in tempo quadratico sul numero di archi del grafo è probabilmente inefficiente, e andrebbe eventualmente testato su grafi fittizi. Inoltre ad un primo esame sembrano esserci esattamente 9999 AS, il che rende la matrice di adiacenza piuttosto sparsa. Tenerlo in considerazione per dopo.

Nelle slide del corso viene data una definizione formale di percorso che non risolve l'ambiguità. Abbandonare il punto facoltativo.

Nuovo obiettivo: scrivere un file setup.cpp che parsi *.all-paths.bz2, costruisca il grafo, ne determini le sue componenti connesse ed il massimo peso associato ad un arco. Questo file sarà distinto da quello definitivo, e se risulta esserci un'unica componente connessa allora riuserò il codice nel file definitivo mentre se ne trovo più di una creerò un nuovo file di testo con solo gli archi della componente connessa massimale, già pesati e già sortati, per velocizzare la fase successiva di sviluppo.

Per contare le componenti connesse uso un algoritmo dfs, anche se nel file finale vorrò probabilmente usare un'altro algoritmo. Inoltre, per salvare le adiacenze, uso un vettore di vettori di pair<int,int>, dove il primo elemento rappresenta un arco e il secondo il peso relativo a quell'arco (naturale generalizzazione del 

Update dopo aver completato la prima parte di setup.cpp: ci sono parecchie componenti connesse. Nello specifico caso di 19980101.all-paths, la componente connessa più grande ha soltanto 3234 nodi. Inoltre il peso massimo di un arco è 49609, molto maggiore di quanto mi aspettassi in prima analisi. Il prossimo passo sarà contare il numero di archi del grafo finale (ovvero della componente connessa più grande), per poi far produrre al file setup.cpp un nuovo file di testo con il grafo definitivo da usare per il progetto in formato più utile.
Conclusione: occorre comprimere sia i nodi della componente connessa che i pesi usando un'hash. Dovrebbe bastare la struttura unordered_map di cpp. Poi entrambe le mappe vanno trasferite al file soluzione.cpp, assieme alla lista di archi (in ordine di peso dal minore al maggiore). 

Correzione: comprimere i pesi dovrebbe essere inutile. La versione definitiva di setup.cpp legge il file .all-paths, trova la componente connessa più grossa, e scrive in un singolo file (in quest'ordine): il numero di nodi del grafo, i loro indici, il massimo peso di un'arco, il numero di archi, e gli archi in ordine crescente nel peso.

Prima versione della soluzione: carica i dati riguardanti il grafo (come specificato sopra), li salva in un vettore di vettori adj, e fa binary search sul valore di costo(P) controllando con una dfs se i nodi a e b sono connessi nel grafo implicito ottenuto togliendo i nodi con valore strettamente maggiore di costo(P). Questa soluzione è salvata in SoluzioneV1.cpp.
